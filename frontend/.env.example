# URL base da API REST do backend. Apenas variáveis prefixadas com VITE_
# são expostas no código do navegador. Não colocar segredos aqui — tudo
# que está aqui é visível pra qualquer um que inspecionar o bundle.
VITE_API_URL=http://localhost:8000/api/v1
